{
  "//": [
    "Task D — code review. Precision AND recall against a HIDDEN defect manifest.",
    "",
    "The manifest never enters the prompt: the model sees only the patch. Each",
    "seeded defect is matched by any of its keyword aliases appearing in the",
    "review. `noise` lists a harmless style nit that is deliberately present --",
    "flagging it is not penalised, but ranking it above a real defect is, which",
    "is why severity_order is scored separately.",
    "",
    "Precision matters as much as recall: a model that lists twenty speculative",
    "concerns to guarantee hitting the real ones is not doing review."
  ],
  "schema_version": 1,
  "tasks": [
    {
      "id": "rev01_seeded_patch",
      "statement": "Review this patch. Report every defect you find with its severity and the reason. Do NOT rewrite the code.\n\n--- a/auth/session.py\n+++ b/auth/session.py\n@@\n-def verify(token, secret):\n-    expected = hmac.new(secret, token.payload, 'sha256').hexdigest()\n-    return hmac.compare_digest(expected, token.sig)\n+def verify(token, secret):\n+    expected = hmac.new(secret, token.payload, 'sha256').hexdigest()\n+    return expected == token.sig\n@@\n-def load_user(uid):\n-    return db.query(User).filter_by(id=uid).one_or_none()\n+def load_user(uid):\n+    return db.execute(f\"SELECT * FROM users WHERE id = {uid}\").fetchone()\n@@\n-CACHE_TTL = 300\n+CACHE_TTL = 300  # seconds\n@@\n-def list_all(users):\n-    return {u.id: u for u in users}\n+def list_all(users):\n+    out = {}\n+    for u in users:\n+        out[u.id] = [x for x in users if x.org == u.org]\n+    return out\n@@\n-def revoke(uid, *, cascade=False):\n+def revoke(uid, cascade):\n     ...",
      "defects": [
        {"id": "timing_attack", "severity": "critical",
         "aliases": ["compare_digest", "timing", "constant time", "constant-time", "timing attack"]},
        {"id": "sql_injection", "severity": "critical",
         "aliases": ["sql injection", "sqli", "f-string", "parameteris", "parameteriz", "interpolat"]},
        {"id": "quadratic_perf", "severity": "high",
         "aliases": ["o(n^2)", "o(n2)", "quadratic", "nested loop", "performance regression"]},
        {"id": "api_break", "severity": "high",
         "aliases": ["keyword-only", "keyword only", "signature change", "breaking change", "backward"]},
        {"id": "missing_test", "severity": "medium",
         "aliases": ["no test", "missing test", "untested", "test coverage"]}
      ],
      "noise": [
        {"id": "comment_style", "aliases": ["# seconds", "comment"]}
      ]
    }
  ]
}
