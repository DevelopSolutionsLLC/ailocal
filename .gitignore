# Secrets
.env
*.pem
*.key

# Python
__pycache__/
*.py[cod]
.venv/
build/
dist/
*.egg-info/

# Guard only: runtime state belongs in $AILOCAL_STATE. These stay so a path
# that regresses to a repo-relative default cannot commit runtime state.
/captures/
/logs/

# Local tool state
.DS_Store
.claude/
.grepai/
